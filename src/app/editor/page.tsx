import { EditorLayout } from "@/components/editor/EditorLayout";

export const metadata = {
  title: "Courtroom Builder | AI Courtroom LMS",
  description: "Visual 3D courtroom scene editor — drag, drop, and configure your AI courtroom environment.",
};

export default function EditorPage() {
  return <EditorLayout />;
}
